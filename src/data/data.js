export const reps = ["60", "50", "20", "30", "15", "10"];
export const secs = ["60", "50", "20", "30", "15", "10"];

export let randomJumpingJacks = reps[Math.floor(Math.random() * reps.length)];
export let randomCrunches = reps[Math.floor(Math.random()*reps.length)];
export let randomSquats = reps[Math.floor(Math.random()*reps.length)];
export let randomPushUps = reps[Math.floor(Math.random()*reps.length)];
export let randomWallSits = reps[Math.floor(Math.random()*reps.length)];
export let randomBurpees = reps[Math.floor(Math.random()*reps.length)];
export let randomArmCircles = reps[Math.floor(Math.random()*reps.length)];

